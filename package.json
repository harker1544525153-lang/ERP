{
  "name": "erp-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install",
    "start": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  }
}
